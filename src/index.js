import "./components/HauntedMansion.js";
import "./components/GhostUser.js";
import "./components/DeadTree.js";
import "./components/TombStone.js";
import "./components/BatUser.js";
import "./components/CementeryZone.js";
import "./components/SkyZone.js";
// import { addUser } from "./modules/addUser.js";

/*
// eslint-disable-next-line
const client = new tmi.Client({ channels: ["manzdev"] });
client.connect();

client.on("message", (channel, tags, message) => {
  const username = tags.username;
  const color = tags.color ?? "#fff";
  const isCommand = message.startsWith("!monster");

  isCommand && addUser({ username, color });
  // console.log(username, color, message);
});
*/
